# 20260901_hoi4d_2d_dct_val_panels — GT | hoi4d-2d on HOI4D val

10 val samples (seed 42421), columns GT | 20260901_hoi4d_2d_dct
(best-epoch28). IMAGES CURRENTLY ON THE VOLUME ONLY (rendered on the
training pod, which was deleted before the copy; fetch via the mirror
on the next dev-pod start). manifest.yaml has the exact command.

Caveats: the vis tool used its own 0.1 scene split (150/17) vs the
run's 0.15 — treat panels as qualitative; some "val" scenes may
overlap the training split.
