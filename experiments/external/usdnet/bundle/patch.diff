diff --git a/conf/loss/set_criterion_articulation.yaml b/conf/loss/set_criterion_articulation.yaml
index 190965d..dc9fe4a 100644
--- a/conf/loss/set_criterion_articulation.yaml
+++ b/conf/loss/set_criterion_articulation.yaml
@@ -11,4 +11,9 @@ oversample_ratio: 3.0
 importance_sample_ratio: 0.75
 class_weights: -1
 regular_arti_loss: True
-use_mov_mask_for_interaction_loss: False
\ No newline at end of file
+use_mov_mask_for_interaction_loss: False
+screw_weight: 0.0
+screw_mode: replace
+screw_min_radius: 0.1
+screw_term: h1
+screw_cap: 0.0
diff --git a/models/criterion.py b/models/criterion.py
index 8574a30..1529a08 100644
--- a/models/criterion.py
+++ b/models/criterion.py
@@ -6,6 +6,7 @@ MaskFormer criterion.
 """
 
 import torch
+from screw_loss import closed_form_screw_loss  # SCREW_PATCH
 import torch.nn.functional as F
 from torch import nn
 
@@ -135,6 +136,7 @@ class SetCriterion(nn.Module):
         class_weights,
         regular_arti_loss = True,
         use_mov_mask_for_interaction_loss = False,
+        screw_weight = 0.0, screw_mode = 'replace', screw_min_radius = 0.1, screw_term = 'h1', screw_cap = 0.0,
     ):
         """Create the criterion.
         Parameters:
@@ -152,6 +154,11 @@ class SetCriterion(nn.Module):
         self.eos_coef = eos_coef
         self.losses = losses
         self.regular_arti_loss = regular_arti_loss
+        self.screw_weight, self.screw_mode = float(screw_weight), screw_mode
+        self.screw_min_radius, self.screw_term = float(screw_min_radius), screw_term
+        self.screw_cap = float(screw_cap)
+        self._screw_acc = [0.0, 0.0, 0.0, 0]  # sum_screw, sum_theirs, sum_axis1cos, n
+        print('SCREW_PATCH: weight', self.screw_weight, 'mode', self.screw_mode, 'min_radius', self.screw_min_radius, 'term', self.screw_term)
         print("self.regular_arti_loss: ", self.regular_arti_loss)
         empty_weight = torch.ones(self.num_classes + 1)
         empty_weight[-1] = self.eos_coef
@@ -249,7 +256,27 @@ class SetCriterion(nn.Module):
                         # print("target_axis shape: ", target_axis.shape)
                         dist_gt_axis = torch.norm(dist_vec_gt_axis) / torch.norm(target_axis)
                         dist_pred_axis = torch.norm(dist_vec_pred_axis) / (torch.norm(pred_axis) + torch.tensor(1e-6, device=map_origins.device))
-                        loss_translations_item.append(dist_gt_axis + dist_pred_axis)
+                        theirs_item = dist_gt_axis + dist_pred_axis
+                        if self.screw_weight > 0 and 'mov_parts_centers' in targets[batch_id]:
+                            p_star = targets[batch_id]['mov_parts_centers'][target_id].to(pred_origin).float()
+                            n_gt = F.normalize(target_axis.float(), dim=0)
+                            rel = p_star - target_origin.float()
+                            lever = rel - (rel * n_gt).sum() * n_gt
+                            if lever.norm() >= self.screw_min_radius:
+                                pos, der = closed_form_screw_loss(
+                                    torch.ones(1, device=pred_origin.device), pred_axis[None], pred_axis[None],
+                                    pred_origin[None], p_star[None], target_axis[None], target_origin[None], p_star[None])
+                                capf = (lambda v: v.clamp(max=self.screw_cap)) if self.screw_cap > 0 else (lambda v: v)
+                                term = capf(der[0]) if self.screw_term == 'h1' else (capf(pos[0]) if self.screw_term == 'pos' else capf(pos[0]) + capf(der[0]))
+                                item = self.screw_weight * term + (theirs_item if self.screw_mode == 'add' else 0.0)
+                                a = self._screw_acc; a[0] += float(term); a[1] += float(theirs_item); a[3] += 1
+                                if a[3] % 500 == 0:
+                                    print(f"SCREW_STATS n={a[3]} mean_screw_term={a[0]/a[3]:.4f} mean_theirs_linedist={a[1]/a[3]:.4f}")
+                            else:
+                                item = theirs_item
+                            loss_translations_item.append(item)
+                        else:
+                            loss_translations_item.append(theirs_item)
                     else:
                         # translation, don't care about the origin
                         loss_translations_item.append(torch.tensor(0.0, device=map_origins.device))
