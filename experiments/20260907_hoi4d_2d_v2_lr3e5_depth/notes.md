# 20260907_hoi4d_2d_v2_lr3e5_depth

**Goal:** the sweep winner (DCT head, lr 3e-5, 100 ep) on the depth-complete v2 rebuild. In the sweep the pred_depth anchor skipped every depth-less row, so the trajectory term only trained on C4/C6; this arm is the fair reference for the teacher-forced and fullfix arms.

**Result:** (pending)
